--- /src/main/java/com/vaadin/client/ui/combobox/ComboBoxConnector.java	2015-10-08 09:25:56.000000000 +0700
+++ /src/main/java/com/vaadin/client/ui/combobox/ComboBoxConnector.java	2015-10-08 09:20:18.000000000 +0700
@@ -24,17 +24,15 @@
 import com.vaadin.client.UIDL;
 import com.vaadin.client.ui.AbstractFieldConnector;
 import com.vaadin.client.ui.SimpleManagedLayout;
-import com.vaadin.client.ui.VFilterSelect;
-import com.vaadin.client.ui.VFilterSelect.FilterSelectSuggestion;
+import com.vaadin.client.ui.VFilterSelectPatched;
+import com.vaadin.client.ui.VFilterSelectPatched.FilterSelectSuggestion;
 import com.vaadin.client.ui.menubar.MenuItem;
-import com.vaadin.shared.ui.Connect;
 import com.vaadin.shared.ui.combobox.ComboBoxConstants;
 import com.vaadin.shared.ui.combobox.ComboBoxState;
 import com.vaadin.shared.ui.combobox.FilteringMode;
-import com.vaadin.ui.ComboBox;
 
-@Connect(ComboBox.class)
-public class ComboBoxConnector extends AbstractFieldConnector implements
+//@Connect(ComboBox.class)
+public class ComboBoxConnectorPatched extends AbstractFieldConnector implements
         Paintable, SimpleManagedLayout {
 
     /*
@@ -160,9 +158,9 @@
                     getWidget().totalMatches);
             getWidget().waitingForFilteringResponse = false;
             if (!getWidget().popupOpenerClicked
-                    && getWidget().selectPopupItemWhenResponseIsReceived != VFilterSelect.Select.NONE) {
+                    && getWidget().selectPopupItemWhenResponseIsReceived != VFilterSelectPatched.Select.NONE) {
                 // we're paging w/ arrows
-                if (getWidget().selectPopupItemWhenResponseIsReceived == VFilterSelect.Select.LAST) {
+                if (getWidget().selectPopupItemWhenResponseIsReceived == VFilterSelectPatched.Select.LAST) {
                     getWidget().suggestionPopup.menu.selectLastItem();
                 } else {
                     getWidget().suggestionPopup.menu.selectFirstItem();
@@ -182,7 +180,7 @@
                         activeMenuItem.getText().length()
                                 - getWidget().lastFilter.length());
 
-                getWidget().selectPopupItemWhenResponseIsReceived = VFilterSelect.Select.NONE; // reset
+                getWidget().selectPopupItemWhenResponseIsReceived = VFilterSelectPatched.Select.NONE; // reset
             }
             if (getWidget().updateSelectionWhenReponseIsReceived) {
                 getWidget().suggestionPopup.menu
@@ -261,8 +259,8 @@
     }
 
     @Override
-    public VFilterSelect getWidget() {
-        return (VFilterSelect) super.getWidget();
+    public VFilterSelectPatched getWidget() {
+        return (VFilterSelectPatched) super.getWidget();
     }
 
     @Override
@@ -272,7 +270,7 @@
 
     @Override
     public void layout() {
-        VFilterSelect widget = getWidget();
+        VFilterSelectPatched widget = getWidget();
         if (widget.initDone) {
             widget.updateRootWidth();
         }